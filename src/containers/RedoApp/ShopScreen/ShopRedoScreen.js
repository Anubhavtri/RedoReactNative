import { StyleSheet, Text, View } from 'react-native'
import React from 'react'
import { SafeAreaView } from 'react-native-safe-area-context';
const ShopRedoScreen = () => {
  return (
    <SafeAreaView style={{ alignItems: 'center',justifyContent: 'center',marginTop:200}}>
      <Text>ShopRedoScreen</Text>
    </SafeAreaView>
  )
}

export default ShopRedoScreen

const styles = StyleSheet.create({})