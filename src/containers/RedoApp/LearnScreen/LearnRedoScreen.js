import { StyleSheet, Text, View } from 'react-native'
import React from 'react'
import { SafeAreaView } from 'react-native-safe-area-context';
const LearnRedoScreen = () => {
  return (
    <SafeAreaView style={{ alignItems: 'center',justifyContent: 'center',marginTop:200}}>
      <Text>LearnRedoScreen</Text>
    </SafeAreaView>
  )
}

export default LearnRedoScreen

const styles = StyleSheet.create({})