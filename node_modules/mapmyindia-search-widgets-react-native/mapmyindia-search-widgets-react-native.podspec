require "json"

package = JSON.parse(File.read(File.join(__dir__, "package.json")))

Pod::Spec.new do |s|
  s.name         = "mapmyindia-search-widgets-react-native"
  s.version      = package["version"]
  s.summary      = package["description"]
  s.description  = <<-DESC
                  react-native-mapmyindia-react-native-place-picker
                   DESC
  s.homepage     = "https://github.com/github_account/react-native-mapmyindia-react-native-place-picker"
  # brief license entry:
  s.license      = "MIT"
  # optional - use expanded license entry instead:
  # s.license    = { :type => "MIT", :file => "LICENSE" }
  s.authors      = { "Your Name" => "yourname@email.com" }
  s.platforms    = { :ios => "9.0" }
  s.source       = { :git => "https://github.com/github_account/react-native-mapmyindia-react-native-place-picker.git", :tag => "#{s.version}" }
  s.swift_version = '5.2'
  s.source_files = "ios/**/*.{h,c,m,swift}"
  s.requires_arc = true

  s.dependency "React"
  s.dependency 'MapmyIndiaUIWidgets/Base', '~> 0.3.10'
  s.dependency "MapmyIndiaMaps/Base"
  s.dependency 'MapmyIndiaAPIKit'

  # ...
  # s.dependency "..."
end

