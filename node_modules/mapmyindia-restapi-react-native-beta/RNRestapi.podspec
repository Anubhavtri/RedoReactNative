require 'json'

package = JSON.parse(File.read(File.join(__dir__, 'package.json')))

Pod::Spec.new do |s|
  s.name         = "RNRestapi"
  s.version      = "1.0.0"
  s.summary      = "RNRestapi"
  s.description  = <<-DESC
                  RNRestapi
                   DESC
  s.homepage     = "https://github.com/MapmyIndia/mapmyindia-restapi-react-native-beta#readme"
  s.license      = "MIT"
  # s.license      = { :type => "MIT", :file => "FILE_LICENSE" }
  s.author             = { "author" => "author@domain.cn" }
  s.platform     = :ios, "9.0"
  s.source       = { :git => "https://github.com/author/RNRestapi.git", :tag => "master" }
  s.source_files  = "ios/RNRestapi/**/*.{h,m}"
  s.requires_arc = true


  s.dependency "React"
  #s.dependency "others"

end

  
